# ── Sunucu ───────────────────────────────────────────────────────────────────
PORT=3000
NODE_ENV=production

# Uygulamanın dış dünyadan görünen adresi.
# Stripe, ödeme sonrası kullanıcıyı buraya geri gönderir.
PUBLIC_URL=https://meetapp.example.com

# Oturum çerezlerini imzalar. Üretimde mutlaka uzun ve rastgele olmalı.
SESSION_SECRET=uzun-rastgele-bir-dize-buraya

# ── Uygulama sahibi ──────────────────────────────────────────────────────────
# Tüm katılım ücretleri bu hesapta toplanır.
OWNER_NAME=MeetApp
OWNER_EMAIL=owner@meetapp.app
OWNER_PASSWORD=owner1234

# ── Dil ──────────────────────────────────────────────────────────────────────
# Sunucu mesajlarının varsayılan dili (tr | en). Kullanıcı arayüzden başka bir
# dil seçerse istemci bunu X-Lang başlığıyla gönderir ve seçim öne geçer.
DEFAULT_LANG=tr

# ── Para ─────────────────────────────────────────────────────────────────────
# Ülkene göre değiştir. Örnekler:
#   Çekya  → CURRENCY=CZK  CURRENCY_SYMBOL=Kč
#   Euro   → CURRENCY=EUR  CURRENCY_SYMBOL=€
CURRENCY=TRY
CURRENCY_SYMBOL=₺

# Uygulama sahibinin payı (0–1). 0.10 = %10 komisyon,
# kalan %90 organizatöre ödenecek tutar olarak raporlanır.
COMMISSION_RATE=0.10

# ── Ödeme ────────────────────────────────────────────────────────────────────
# Bu anahtar tanımlıysa gerçek Stripe Checkout kullanılır ve para
# doğrudan bu anahtarın ait olduğu (uygulama sahibinin) hesabına geçer.
# Boş bırakılırsa uygulama demo kart ekranıyla çalışır.
# STRIPE_SECRET_KEY=sk_live_...

# Webhook imzasını doğrulamak için (Stripe panelinde webhook eklerken verilir).
# Tanımlı değilse ödeme yalnızca kullanıcı tarayıcıya geri döndüğünde onaylanır;
# kullanıcı sekmeyi kapatırsa kayıt "ödeme bekliyor"da kalır.
# STRIPE_WEBHOOK_SECRET=whsec_...

# ── Organizatörlere otomatik ödeme (Stripe Connect) ──────────────────────────
# Açıkken organizatörün payı, ödeme anında doğrudan kendi Stripe hesabına
# geçer; senin komisyonun otomatik ayrılır. Kapatırsan tüm para sende toplanır
# ve organizatörlere elle aktarırsın.
STRIPE_CONNECT=true

# Organizatör hesaplarının açılacağı ülke (ISO 3166-1 alfa-2).
CONNECT_COUNTRY=CZ

# ── E-posta ──────────────────────────────────────────────────────────────────
# Tanımlı değilse postalar gönderilmez, içerikleri konsola yazılır — geliştirme
# için yeterli. Üretimde bir SMTP sağlayıcısı bağla.
# SMTP_URL=smtps://kullanici:parola@smtp.saglayici.com:465
MAIL_FROM=MeetApp <no-reply@meetapp.app>

# Hatırlatma postası etkinlikten kaç saat önce gitsin.
REMINDER_HOURS=24

# ── Hız sınırları ────────────────────────────────────────────────────────────
# Varsayılanlar bir insan için fazlasıyla geniş. Ortak bir IP arkasındaki
# kalabalık bir kurulumda yükseltmen gerekebilir.
RATE_LIMIT_AUTH=30
RATE_LIMIT_PAY=20

# ── Veritabanı ───────────────────────────────────────────────────────────────
# Veritabanı VE oturum dosyaları bu yolun bulunduğu klasöre yazılır.
# Sunucuda bu klasör kalıcı diske (volume) bağlı olmalıdır; yoksa her
# dağıtımda etkinlikler, kayıtlar ve oturumlar silinir.
# Varsayılan: ./meetapp.db   ·   Docker imajında: /data/meetapp.db
# DB_PATH=/data/meetapp.db
